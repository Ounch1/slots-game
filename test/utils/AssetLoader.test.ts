import { AssetLoader } from '@/utils/AssetLoader';

describe('AssetLoader', () => {
	let loader: AssetLoader;

	beforeEach(() => {
		loader = new AssetLoader();
	});

	test.todo('should initialize Assets properly');
});